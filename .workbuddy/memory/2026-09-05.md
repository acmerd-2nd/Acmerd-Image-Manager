# 2026-09-05

- Owner 裁决 PB-1 报告存在"实施范围事实冲突"（05 的"生产零触碰" vs 08 的实际执行）。
- 判定走第二分支：Stage 1/2 已实际执行 → 新建 `docs/v1.1/09-factual-clarification-production-evidence.md`（授权/执行/部署三态表 + Stage 2 六项证据）。
- 新增 `scripts/v11-post-closure-verify.mjs`：当前时点生产复核 6/6 PASS（provider=github、raw 206、Storage 原件保留、认证后下载 302→raw、health 200）。
- 文档重组：05 恢复原报告正文+修正注记；裁决原文存 05b；P4 401 判定为 requireUser 登录门（预期）。
- 长期规则入库：冲突重试不可替代 lease（写入项目 MEMORY.md）。
- PB-1 Gate Closure 正式成立；commit + push origin/main。
- Owner 指令"Phase C 新 Gate"→ 产出 `docs/v1.1/10-phase-c-design-gate.md`（PENDING OWNER REVIEW，零代码）。
- 现状核实：Worker 零 credits 调用（PC-4 是最重接线）；0010 RPC 含 H2 幂等+unlimited 旁路；settings anon 可读；collections RLS 就位；account_origin 就位。
- Gate 要点：PC-1..PC-7 子阶段（PC-4 后 STOP）；Q1-Q5 裁决点（单图不加 HEAD 探针、幂等 key 前端 uuid、无归属 asset 不进公域、GoTrue 现状、沙箱验证）；残余风险=GoTrue 直连 signup 绕过 Worker gate。
- git push 仍未上 origin/main（origin 落后多版，10 号 Gate 文档随下轮推送）。
- Owner 要求建"【进度看板】实时更新.txt"（Phase C 目标 + 状态标记 + Agent 接手须知），已建并 commit 64c8e7c。
- git push 根因终于定位并解决：GCM helper 非交互挂起（-c credential.helper= 空值重置）+ 非 ASCII 路径助手无法执行（移至 %TEMP%）。push 成功 5e74187..64c8e7c，V1.1 全部提交上远端。跨项目坑已记入 ~/.workbuddy/MEMORY.md。
- 看板随即更新 push ✅ 状态。

## 同日续：换号接手，PC-1 全量接线完成（PC-1 CLOSED）
- 接手上下文：Phase C Gate APPROVED（Q1–Q5 按建议），上轮 PC-1 part1/2 已提交，part 3/3（admin 扫描）工作区未提交。
- 验证起点：typecheck 绿；扫描发现 admin 各页仍有大量硬编码文案（上轮已建 key 未接线）。
- 完成接线：AdminNew/Tags/Assets/AuditLogs/Users 页残留文案 → t()；AppShell 品牌「探知」与 footer tagline → home.brand / home.footerTagline。
- features 层错误文案 i18n 化（非 React 场景 t()）：admin api（错误码→admin.api.*）、downloads/api（DownloadError 全路径）、search/api（校验器 errors.*）、assets/github+storage（校验/HTTP 兜底 download.*）。模式沿用 part 2：异常消息就近 t()，页面零改动。
- 字典新增：home.brand/footerTagline、download.*（8 个 API 文案 key）、errors.queryTooLong/tooManyTags、admin.api.*（8）、admin.assets.operationFailed/deleteFailed/restoreHint、admin.misc.githubCleanupFailed、admin.auditPage.filter*（5）、admin.editor.namePlaceholder/slugAutoFromName/newPageHint、adminExtra.unnamed；zh/en 严格同构（typecheck 强制）。
- 修复 2 个交接遗留 bug：① AdminTagsPage ConfirmDialog description 模板串漏 $（会字面渲染 "{t(...)}"）② AdminAuditLogsPage 筛选按钮 labelKey 为 'asset.' 等非法 key（原样显示）。
- 验证：typecheck 绿（含 Dictionary 同构）、vite build 绿（入口 gzip 138.51 kB）。
- commit 9ee0821（16 文件，含上轮工作区遗留的 Editor/Dashboard/Storage 接线）。PC-1 三段：f3fd712 / 68b706e / 9ee0821。
- 下一步：PC-2 Collection UI（首页卡片 + /collection/:slug + Admin CRUD + 排序/归档/设封面；RLS 0012 已就位；Q3 裁决=无归属 asset 不进公域）。

## 同日续 2：PC-2 Collection UI 完成（16/16 冒烟 PASS）
- 公开侧：首页改列 published_collections 卡片（cover 经 makeImageUrl；无图 FolderOpen 占位）+ 未归组已发布资产折叠区（Q3 裁决落地：不进主浏览流）；新路由 /collection/:slug 列双层 published 资产。
- Admin：新 AdminCollectionsPage（创建/发布前封面校验/归档/恢复/删除、sort_order 上下移、成员添加/移出带 cover 守卫提示）；AdminLayout 导航加 Collections（FolderTree）。
- Worker 新端点（service_role 原子 + writeAudit）：POST /api/admin/collections（created）、PATCH /api/admin/collections/:id（updated/published/archived 对齐 0012 语义）、DELETE（deleted；FK SET NULL 回归未归组）、POST /api/admin/collections/assign（cover-in-use 守卫 → 409 collection_guard）。审计 allowlist 0013 已含全部 collection.*，零迁移。
- 前端 API 层 src/features/collections/api.ts（公开视图读 + admin Worker 写）；AssetCard 统计标签 i18n 化（Images/Languages → asset.*)。
- 冒烟 scripts/v11-pc2-smoke.mjs（隔离库 0001–0014 全量，一次性库 DROP）：16/16 PASS——视图语义（空 shell 不出现/draft 不计数/cover 暴露/sort_order）、RLS 矩阵（anon 只读 published、写 0 行拒绝）、H1 双守卫（COLLECTION_COVER_MISMATCH/COLLECTION_COVER_IN_USE）、删除回归未归组、审计落行。
- 技术要点：SET LOCAL role 只在显式事务内生效（自动提交是 no-op）——anon 模拟用 BEGIN→SET LOCAL→ROLLBACK，admin 写用同模式+COMMIT 以走 RLS admin 路径产生审计；RLS 拒绝语义是 0 行受影响而非抛错；同事务内语句失败后须 ROLLBACK 再测下一条。
- typecheck+build 绿。commit f914bbf（13 文件 +1365）。
- 下一步：PC-3 Schedule（site_settings.schedule_navigation_enabled 显隐 + Coming Soon 页 + Admin 开关，0011 anon 可读已就位）。

## 同日续 3：PC-3 Schedule 完成
- 公开 /schedule Coming Soon 页（总纲 §23：产品态非 403；未来 schedule.items 超范围）；AppShell 导航项由 site_settings.schedule_navigation_enabled 控制显隐（anon 直读 0011 grants；读取失败兜底隐藏）。
- Worker GET/PATCH /api/admin/settings：5 key allowlist + boolean/非负整数校验；逐 key service_role PATCH（updated_by 落当前 admin）；settings.updated 审计（0013 allowlist 已含）。PC-6 复用同一端点加 Registration + 3 costs 控件。
- Admin Dashboard 顶部加 Platform Controls 卡（PC-3 范围仅 Schedule 开关，苹果风 switch role=switch）。
- 新文件 src/features/settings/api.ts（公开只读 SiteSettings）+ SchedulePage；admin api 加 getPlatformSettings/updatePlatformSettings。
- typecheck+build 绿。commit 90f3428（9 文件 +286）。
- 下一步：PC-4 Credits 接线（单图/ZIP/Package 三链路 × 0010 RPC；Q1 不加 HEAD 探针、Q2 前端 uuid 幂等键）→ 隔离库+沙箱全矩阵 → STOP 等 Owner。
## 同日续 4：PC-4 沙箱环境修复 + 重跑 31/33（交接准备）
- 沙箱三轮排障，最终根因＝.dev.vars 遗留 Phase B 演练仓 image-dryrun-sandbox，wrangler dev 时优先于 wrangler.toml → 测试图写进 private 演练仓，raw URL 指向生产仓必 404 → ZIP 502。已对齐生产仓（.dev.vars 被 gitignore，仅本机生效，交接必读）。
- 代理坑：环境强制 http_proxy=127.0.0.1:65235（死代理）；curl 须 --noproxy，Node fetch 默认忽略代理直连；runner 加 NO_PROXY='*' 双保险。
- 端口坑：4173 有跨会话僵尸 listener 杀不掉且复活 → 弃用，专用 8787；wrangler dev 跨调用会死 → 必须 scripts/_pc4-runner.sh 单进程树跑。
- 最新实测（aa22e71）：31 PASS / 2 FAIL，仅 W0f 传播等待 404>150s；W7 ZIP 200 PASS 反证 raw 最终可用 + worker 出网正常。waitGithubRaw 已提到 420s（87b2260，未重跑验证）。
- 交接文档：.qoder/specs/PC4-收口_沙箱31of33与W0f收尾_20260905.md；本地 5 commit（d6ad0fc/87b2260/ace8d58/d44282e/aa22e71），未 push。
- 临时文件 _diag-w7.mjs / .pc4-*.log 已删。下一步＝下一任 Agent：重跑沙箱全绿 → Step 4/6/7 → STOP 等 Owner。
