import type { Dictionary } from './zh'

/** V1.1 D2：en 字典。key 必须与 zh 严格同构（编译期强制）。 */
export const en: Dictionary = {
  common: {
    loading: 'Loading…',
    error: 'Something went wrong. Please try again.',
    retry: 'Retry',
  },
}
